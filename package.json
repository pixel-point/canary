{
  "name": "harness-icons",
  "scripts": {
    "deps": "pnpm -r install",
    "clean": "pnpm -r --parallel clean",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "fmt": "pnpm -r fmt",
    "dev": "pnpm --filter docs dev",
    "preinstall": "npx only-allow pnpm"
  }
}
