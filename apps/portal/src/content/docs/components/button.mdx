---
title: Button
description: Interactive button component for user actions
---

The `Button` component allows users to trigger actions, submit forms, and navigate through the application.

import { DocsPage } from "@/components/docs-page";
import { Button } from "@harnessio/ui/components";

<DocsPage.ComponentExample
  client:only
  code={`<div className="flex flex-col gap-8">
  {/* Variants */}
  <div>
    <h5 className="text-lg font-semibold mb-3">Variants</h5>
    <div className="flex flex-wrap items-end gap-3">
      <Button variant="primary">Primary</Button>
      <Button variant="secondary">Secondary</Button>
      <Button variant="outline">Outline</Button>
      <Button variant="ghost">Ghost</Button>
      <Button variant="ai">AI</Button>
      <Button variant="link">Link</Button>
    </div>
  </div>

{/* Themes */}

<div>
  <h5 className="text-lg font-semibold mb-3">Themes</h5>
  <div className="flex flex-wrap items-end gap-3">
    <Button theme="default">Default</Button>
    <Button theme="success">Success</Button>
    <Button theme="danger">Danger</Button>
  </div>
</div>

{/* Sizes */}

<div>
  <h5 className="text-lg font-semibold mb-3">Sizes</h5>
  <div className="flex flex-wrap items-end gap-3">
    <Button size="lg">Large</Button>
    <Button>Default</Button>
    <Button size="sm">Small</Button>
  </div>
</div>

{/* Rounded Buttons */}

<div>
  <h5 className="text-lg font-semibold mb-3">Rounded Buttons</h5>
  <div className="flex flex-wrap items-end gap-3">
    <Button rounded>Rounded</Button>
    <Button rounded variant="outline">
      Rounded Outline
    </Button>
  </div>
</div>

{/* Loading State */}

<div>
  <h5 className="text-lg font-semibold mb-3">Loading State</h5>
  <div className="flex flex-wrap items-end gap-3">
    <Button loading>Loading</Button>
    <Button loading variant="primary">
      Loading Primary
    </Button>
  </div>
</div>

{/* Disabled Buttons */}

<div>
  <h5 className="text-lg font-semibold mb-3">Disabled Buttons</h5>
  <div className="flex flex-wrap items-end gap-3">
    <Button disabled>Disabled</Button>
    <Button disabled variant="secondary">
      Disabled Secondary
    </Button>
  </div>
</div>

{/* Icon Only Buttons */}

<div>
  <h5 className="text-lg font-semibold mb-3">Icon Only Buttons</h5>
  <div className="flex flex-wrap items-end gap-3">
    <Button iconOnly>
      <Icon name="plus" />
    </Button>
    <Button iconOnly variant="secondary">
      <Icon name="branch" />
    </Button>
  </div>
</div>

</div>`}
/>

## Usage

```typescript jsx
import { Button } from '@harnessio/ui/components'

// Basic usage
<Button>Default Button</Button>

// Variants
<Button variant="primary">Primary Button</Button>
<Button variant="secondary">Secondary Button</Button>
<Button variant="outline">Outline Button</Button>
<Button variant="ghost">Ghost Button</Button>
<Button variant="ai">AI Button</Button>
<Button variant="link">Link Button</Button>

// Themes
<Button theme="default">Default Theme</Button>
<Button theme="success">Success Theme</Button>
<Button theme="danger">Danger Theme</Button>

// Sizes
<Button size="lg">Large Button</Button>
<Button>Default Size Button</Button>
<Button size="sm">Small Button</Button>

// Rounded
<Button rounded>Rounded Button</Button>
<Button rounded variant="outline">Rounded Outline Button</Button>

// Icon Only
<Button iconOnly>
  <Icon name="plus" />
</Button>
<Button iconOnly variant="secondary">
  <Icon name="check" />
</Button>

// Loading
<Button loading>Loading Button</Button>
<Button loading variant="primary">Loading Primary Button</Button>

// Disabled
<Button disabled>Disabled Button</Button>
<Button disabled variant="secondary">Disabled Secondary Button</Button>
```

## Props

<DocsPage.PropsTable
  props={[
    {
      name: "variant",
      description: "Visual style of the button.",
      required: false,
      value: "'primary' | 'secondary' | 'outline' | 'ghost' | 'ai' | 'link'",
      defaultValue: "'primary'",
    },
    {
      name: "theme",
      description: "Color theme of the button.",
      required: false,
      value: "'default' | 'success' | 'danger'",
      defaultValue: "'default'",
    },
    {
      name: "size",
      description: "Size of the button.",
      required: false,
      value: "'default' | 'sm'",
      defaultValue: "'default'",
    },
    {
      name: "rounded",
      description: "Applies rounded corners to the button.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "iconOnly",
      description: "Optimizes the button styling for displaying only an icon.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "loading",
      description: "Shows a loading spinner and disables the button.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "disabled",
      description: "Disables the button.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "asChild",
      description: "Passes the component styling to a child element.",
      required: false,
      value: "boolean",
      defaultValue: "false",
    },
    {
      name: "className",
      description: "Additional CSS class names.",
      required: false,
      value: "string",
    },
    {
      name: "children",
      description: "Content to display inside the button.",
      required: true,
      value: "ReactNode",
    },
  ]}
/>
