---
title: Toast
description: Toast component
---

The `Toast` component is used to display notifications to the user. The `Toast.Root` component can be used either controlled or uncontrolled. When used uncontrolled, the `Toast.Root` component automatically handles the open state of the toast. When used controlled, the `Toast.Root` component can be controlled by providing the `open` and `onOpenChange` props.

import { DocsPage } from "../../../components/docs-page";

<DocsPage.ComponentExample
  client:only
  code={`() => {
    const [show, setShow] = React.useState(false)

    return (
      <Toast.Provider>
        <Button onClick={() => setShow(curr => !curr)}>{show ? 'Hide' : 'Show'} Toast</Button>

        {show && (
          <Toast.Root onOpenChange={setShow} open={show}>
            <Toast.Title>Toast title</Toast.Title>
            <Toast.Description>Toast description</Toast.Description>
            <Toast.Close />
          </Toast.Root>
        )}
        <Toast.Viewport />
      </Toast.Provider>
    )

}`}
/>

## Usage

```typescript jsx
import { Toast } from '@harnessio/ui/components'

//...

return (
  <Toast.Provider> {/* typically placed at the root of the app */}
    <Toast.Root>
      <Toast.Title>Toast title</Toast.Title>
      <Toast.Description>Toast description</Toast.Description>
      <Toast.Close />
    </Toast.Root>
    <Toast.Viewport />
  </Toast.Provider>
)
```

## Anatomy

All parts of the Toast component can be imported and composed as required.

```typescript jsx
<Toast.Provider>
  <Toast.Root>
    <Toast.Title />
    <Toast.Description />
    <Toast.Close />
    <Toast.Action />
  </Toast.Root>
  <Toast.Viewport />
</Toast.Provider>
```

## API Reference

### `Provider`

The `Provider` component is used to provide the Toast context to the rest of the app. It is typically placed at the root of the app.

```typescript jsx
<Toast.Provider
  label="Notification" // [OPTIONAL] label for screen readers
  duration={5000}      // [OPTIONAL] duration of the toast in milliseconds
swipeDirection="right" // [OPTIONAL] direction in which the toast can be swiped to dismiss
swipeThreshold={50}    // [OPTIONAL] distance in pixels at which the toast can be swiped to dismiss
>
  {/* Toast roots and viewport */}
</Toast.Provider>
```

<DocsPage.PropsTable
  props={[
    {
      name: "label",
      description: "Label for screen readers",
      required: false,
      value: "string",
      defaultValue: "Notification",
    },
    {
      name: "duration",
      description: "Duration of the toast in milliseconds",
      required: false,
      value: "number",
      defaultValue: "5000",
    },
    {
      name: "swipeDirection",
      description: "Direction in which the toast can be swiped to dismiss",
      required: false,
      value: "'left' | 'right' | 'up' | 'down'",
      defaultValue: "right",
    },
    {
      name: "swipeThreshold",
      description:
        "Distance in pixels at which the toast can be swiped to dismiss",
      required: false,
      value: "number",
      defaultValue: "50",
    },
  ]}
/>

### `Root`

The `Root` component is used to wrap the content of the toast. The `Root` component can be used either controlled or uncontrolled. When used uncontrolled, the `Root` component automatically handles the open state of the toast. When used controlled, the `Root` component can be controlled by providing the `open` and `onOpenChange` props.

```typescript jsx
<Toast.Root
  variant="default"       // [OPTIONAL] variant of the toast, can be 'default', 'destructive', 'success'
  open                    // [OPTIONAL] whether the toast is open or closed
  onOpenChange={onChange} // [OPTIONAL] callback when the toast is opened or closed
  defaultOpen             // [OPTIONAL] whether the toast is open by default
  forceMount              // [OPTIONAL] whether the toast should be mounted even if it is not open
  className="class"       // [OPTIONAL] className for the root element
>
  <Toast.Title>Toast title</Toast.Title>
  <Toast.Description>Toast description</Toast.Description>
  <Toast.Close />
</Toast.Root>
```

<DocsPage.PropsTable
  props={[
    {
      name: "variant",
      description: "Variant of the toast",
      required: false,
      value: "'default' | 'destructive' | 'success'",
      defaultValue: "default",
    },
    {
      name: "open",
      description: "Whether the toast is open",
      required: false,
      value: "boolean",
    },
    {
      name: "onOpenChange",
      description: "Callback when the toast is opened or closed",
      required: false,
      value: "(open: boolean) => void",
    },
    {
      name: "defaultOpen",
      description: "Whether the toast is open by default",
      required: false,
      value: "boolean",
    },
    {
      name: "forceMount",
      description: "Whether the toast should be mounted even if it is not open",
      required: false,
      value: "boolean",
    },
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
  ]}
/>

### `Title`

The `Title` component is used to display the title of the toast.

```typescript jsx
<Toast.Title
  className="class" // [OPTIONAL] className for the title element
>
  Toast title
</Toast.Title>
```

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
  ]}
/>

### `Description`

The `Description` component is used to display the description of the toast.

```typescript jsx
<Toast.Description
  className="class" // [OPTIONAL] className for the description element
>
  Toast description
</Toast.Description>
```

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
  ]}
/>

### `Close`

The `Close` component is used to close the toast.

```typescript jsx
<Toast.Close
  className="class" // [OPTIONAL] className for the close button element
  asChild           // [OPTIONAL] render the close button as the child
  onClick={onClick} // [OPTIONAL] callback when the close button is clicked
>
  <button>Close</button>
</Toast.Close>
```

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "asChild",
      description: "Render the close button as the child",
      required: false,
      value: "boolean",
    },
    {
      name: "onClick",
      description: "Callback when the close button is clicked",
      required: false,
      value: "() => void",
    },
  ]}
/>

### `Action`

The `Action` component is used to display a call-to-action button in the toast.

```typescript jsx
<Toast.Action
  className="class" // [OPTIONAL] className for the action button element
  asChild           // [OPTIONAL] render the action button as the child
  onClick={onClick} // [OPTIONAL] callback when the action button is clicked
>
  <button>Approve</button>
</Toast.Action>
```

<DocsPage.PropsTable
  props={[
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
    {
      name: "asChild",
      description: "Render the action button as the child",
      required: false,
      value: "boolean",
    },
    {
      name: "onClick",
      description: "Callback when the action button is clicked",
      required: false,
      value: "() => void",
    },
  ]}
/>

### `Viewport`

The `Viewport` component is used to display the toast in the viewport.

```typescript jsx
<Toast.Viewport
  hotkey={['f8']}                  // [OPTIONAL] hotkey to focus the toast
  label="Notifications ({hotkey})" // [OPTIONAL] label for screen readers
  className="class"                // [OPTIONAL] className for the viewport element
/>
```

<DocsPage.PropsTable
  props={[
    {
      name: "hotkey",
      description: "Hotkey to focus the toast",
      required: false,
      value: "string[]",
    },
    {
      name: "label",
      description:
        "Label for the toast viewport to provide context for screen reader users when navigating page landmarks. The available {hotkey} placeholder will be replaced for you.",
      required: false,
      value: "string",
    },
    {
      name: "className",
      description: "Custom class name",
      required: false,
      value: "string",
    },
  ]}
/>
