---
title: HoverCard
description: HoverCard component
---

The `HoverCard` component provides a popup card that appears when hovering over a trigger element. It's useful for displaying additional information without requiring a click.

import { DocsPage } from "../../../components/docs-page";

<DocsPage.ComponentExample
  client:only
  code={`<HoverCard.Root>
    <HoverCard.Trigger>Hover over me</HoverCard.Trigger>
    <HoverCard.Content>
        <h5>Hover Card Content</h5>
        <p>This is the content that appears when hovering.</p>
    </HoverCard.Content>
</HoverCard.Root>`}
/>

## Usage

```typescript jsx
import { HoverCard } from '@harnessio/ui/components'

//...

return (
  <HoverCard.Root>
    <HoverCard.Trigger asChild>
      <button>Hover over me</button>
    </HoverCard.Trigger>
    <HoverCard.Content>
      <h3>Hover Card Content</h3>
      <p>This is the content that appears when hovering.</p>
    </HoverCard.Content>
  </HoverCard.Root>
)
```

## Anatomy

All parts of the `HoverCard` component can be imported and composed as required.

```typescript jsx
<HoverCard.Root>
  <HoverCard.Trigger />
  <HoverCard.Content />
</HoverCard.Root>
```

## API Reference

### `Root`

The `Root` component is the root container for the hover card functionality.

```typescript jsx
<HoverCard.Root
  open                  // [OPTIONAL] Open state of the hover card
  defaultOpen           // [OPTIONAL] Default open state of the hover card
  onOpenChange={onOpen} // [OPTIONAL] Callback when the open state of the card changes
  openDelay={200}       // [OPTIONAL] Delay in milliseconds to show the card
  closeDelay={100}      // [OPTIONAL] Delay in milliseconds to close the card
>
  {/* Trigger and Content */}
</HoverCard.Root>
```

<DocsPage.PropsTable
  props={[
    {
      name: "open",
      description: "Open state of the hover card",
      required: false,
      value: "boolean",
    },
    {
      name: "defaultOpen",
      description: "Default open state of the hover card",
      required: false,
      value: "boolean",
    },
    {
      name: "onOpenChange",
      description:
        "Callback that fires when the open state of the hover card changes",
      required: false,
      value: "(open: boolean) => void",
    },
    {
      name: "openDelay",
      description: "Delay in milliseconds before the hover card appears",
      required: false,
      value: "number",
    },
    {
      name: "closeDelay",
      description: "Delay in milliseconds before the hover card closes",
      required: false,
      value: "number",
    },
  ]}
/>

### `Trigger`

The `Trigger` component wraps the element that will trigger the hover card to appear.

```typescript jsx
<HoverCard.Trigger
  asChild           // [OPTIONAL] render the trigger as the child
  className="class" // [OPTIONAL] Additional CSS classes
>
  <button>Hover over me</button>
</HoverCard.Trigger>
```

<DocsPage.PropsTable
  props={[
    {
      name: "asChild",
      description: "Render the trigger as the child",
      required: false,
      value: "boolean",
    },
    {
      name: "className",
      description: "Additional CSS classes to apply to the trigger",
      required: false,
      value: "string",

    }

]}/>

### `Content`

The `Content` component contains the content that will be displayed when the trigger is hovered over.

```typescript jsx
<HoverCard.Content
  asChild                          // [OPTIONAL] render the content as the child
  forceMount                       // [OPTIONAL] Force mounting when more control is needed
  side="bottom"                    // [OPTIONAL] Side of the trigger, can be "top", "right", "bottom", "left"
  sideOffset={4}                   // [OPTIONAL] Offset from the trigger
  align="center"                   // [OPTIONAL] Alignment of the content, can be "start", "center", "end"
  alignOffset={4}                  // [OPTIONAL] Offset from the alignment
  avoidCollisions                  // [OPTIONAL] Avoid collisions with other elements
  collisionBoundary={[boundaryEl]} // [OPTIONAL] Boundary to use for collision detection
  collisionPadding={4}             // [OPTIONAL] Padding to apply to the collision detection
  sticky="partial"                 // [OPTIONAL] sticky behavior on the align axis, can be "partial" or "always"
  className="class"                // [OPTIONAL] Additional CSS classes
>
  {/* Hover card content */}
</HoverCard.Content>
```

<DocsPage.PropsTable
  props={[
    {
      name: "asChild",
      description: "Render the content as the child",
      required: false,
      value: "boolean",
    },
    {
      name: "forceMount",
      description: "Force mounting when more control is needed",
      required: false,
      value: "boolean",
    },
    {
      name: "side",
      description:
        "Side of the trigger, can be 'top', 'right', 'bottom', 'left'",
      required: false,
      value: "string",
    },
    {
      name: "sideOffset",
      description: "Offset from the trigger",
      required: false,
      value: "number",
    },
    {
      name: "align",
      description: "Alignment of the content, can be 'start', 'center', 'end'",
      required: false,
      value: "string",
    },
    {
      name: "alignOffset",
      description: "Offset from the alignment",
      required: false,
      value: "number",
    },
    {
      name: "avoidCollisions",
      description: "Avoid collisions with other elements",
      required: false,
      value: "boolean",
    },
    {
      name: "collisionBoundary",
      description: "Boundary to use for collision detection",
      required: false,
      value: "Element | Element[]",
    },
    {
      name: "collisionPadding",
      description: "Padding to apply to the collision detection",
      required: false,
      value: "number",
    },
    {
      name: "sticky",
      description:
        'The sticky behavior on the align axis. "partial" will keep the content in the boundary as long as the trigger is at least partially in the boundary whilst "always" will keep the content in the boundary regardless.',
      required: false,
      value: "string",
    },
    {
      name: "className",
      description: "Additional CSS classes to apply to the content",
      required: false,
      value: "string",
    },
  ]}
/>
