---
title: Logos
description: Logo component
---

The `Logo` component renders brand icons from the `simple-icons` package.

import { DocsPage } from "@/components/docs-page";
import { Logo, LogoNameMap } from "@harnessio/ui/components";

<DocsPage.ComponentExample
  client:only
  code={`<>
    <div className="flex flex-wrap gap-4 mt-4">
      {Object.keys(LogoNameMap).map(name => (
        <div key={name} className="flex items-center gap-4">
          <div className="group relative">
            <div className="absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 hidden group-hover:block bg-cn-background-3 text-cn-foreground-1 text-xs rounded py-1 px-2">
              {name}
            </div>
            <div className="p-2 rounded-md bg-cn-background-primary">
              <Logo name={name as keyof typeof LogoNameMap} size={24} />
            </div>
          </div>
        </div>
      ))}
    </div>
  </>`}
/>

<DocsPage.ComponentExample
  client:only
  code={`<>
    <h5>With theme colors</h5>
    <div className="flex flex-wrap gap-4 mt-4">
      {Object.keys(LogoNameMap).map(name => (
        <div key={name} className="flex items-center gap-4">
          <div className="group relative">
            <div className="absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 hidden group-hover:block bg-cn-background-3 text-cn-foreground-1 text-xs rounded py-1 px-2">
              {name}
            </div>
            <div className="p-2 rounded-md bg-cn-background-primary">
              <Logo name={name as keyof typeof LogoNameMap} size={24} original={false} />
            </div>
          </div>
        </div>
      ))}
    </div>
  </>`}
/>

## Usage

```typescript
import { Logo } from '@harnessio/ui/components'

//...

return <Logo name="github" size={48} />
```

## API Reference

### Props

| Name                                                          | Type                                                                          | Default | Description                                                                 |
| ------------------------------------------------------------- | ----------------------------------------------------------------------------- | ------- | --------------------------------------------------------------------------- |
| <p style={{ whiteSpace: 'nowrap' }}><code>original</code></p> | `boolean`                                                                     | `false` | If `true`, the logo will use its brand-specific color. Defaults to `false`. |
| `name`                                                        | <p style={{ whiteSpace: 'nowrap' }}><code>keyof typeof LogoNameMap</code></p> | N/A     | The name of the brand icon to render (must match a key in `LogoNameMap`).   |
| `size`                                                        | `number`                                                                      | `24`    | The size (in pixels) of the logo.                                           |
